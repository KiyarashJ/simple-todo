import { TextEdit } from "std-widgets.slint";
export component styledInput inherits TextEdit {
        horizontal-alignment: center;
        width: 20%;
        height: 5.5%;
        max-height: 200px;
        wrap: word-wrap;
}